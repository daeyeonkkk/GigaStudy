# Calibration Output

This folder is the default local destination for generated calibration evidence bundles.

Typical generated files include:

- calibration summary JSON and Markdown
- threshold-fit report JSON and Markdown
- human-rating evidence bundle JSON and Markdown

These files are local release-review artifacts, not canonical foundation docs.
Do not move them into `PROJECT_FOUNDATION/`.
